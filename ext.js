/* psuedo code
 * colorChange {
 * 		onclick class changes;
 * 		class reverts to original setting;
 * }
 * 
 */
 
function colorChange(element) {
	console.log("clicked");
	element.setAttribute("id", "lightTest");
}
